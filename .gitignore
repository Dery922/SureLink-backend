# Environment secrets — never commit
.env
.env.local
.env.*.local

# Dependencies
node_modules/

# Uploaded provider verification documents (private, served via signed URLs)
uploads/

# Hidden tool directories (AI assistants, editors, etc.)
.*/

# Keep these dot files tracked
!.gitignore
!.env.example
